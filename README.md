# termtetris
Tetris on the command line

## Getting started
Dependencies: a C++17 compatible Compiler, ncursesw, cmake and make

To start just execute: 
`mkdir build && cd build && cmake .. && make && ./termtetris`
